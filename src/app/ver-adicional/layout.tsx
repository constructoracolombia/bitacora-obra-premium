import "../globals.css";

export const metadata = {
  title: "Adicional de Obra - Constructora Colombia",
};

export default function VerAdicionalLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
